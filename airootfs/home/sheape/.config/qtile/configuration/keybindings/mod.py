class Keys:
    class Modifier:
        mod_key = "mod1"
        alt_key = "mod4"
        shift_key = "shift"
        control_key = "control"
        space_key = "space"
        enter = "Return"
        tab = "Tab"

    # This Vim class isn't for any vim or
    # programs that use vim. It is only
    # for the window manager.

    class Vim:
        left = 'y'
        right = 'o'
        down = 'n'
        up = 'e'
